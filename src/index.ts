// Platform resolution picks ./Camera.windows.tsx on react-native-windows and
// ./Camera.tsx (a passthrough to expo-camera) everywhere else.
export * from './Camera'
export { default } from './Camera'
